repos:
  - repo: local
    hooks:
      - id: angular-lint
        name: ng lint
        entry: bash -c 'cd frontend && npm run lint -- --max-warnings=0'
        language: system
        files: ^frontend/.*\.(ts|html|scss|css)$
        pass_filenames: false
        stages: [pre-push]
